true: strict_sequence, annot, debug
<liboconperf> or <src>: include
<**/*.ml> or <**/*.mli> or <**/*.mlpack> or <**/*.ml.depends>: ocaml, package(core, unix, threads), thread
<**/*.odoc>: ocaml, doc
<**/*.{byte,cma}>: ocaml, byte, library, package(core, unix, threads), thread
<**/*.{native,cmxa}>: ocaml, native, library, package(core, unix, threads), thread
<**/*.cmo>: ocaml, byte
<**/*.cmi>: ocaml, byte, native
<**/*.cmx>: ocaml, native
