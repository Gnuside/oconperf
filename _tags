true: strict_sequence, annot, debug
<src/lib> or <src>: include
<**/*.ml> or <**/*.mli> or <**/*.mlpack> or <**/*.ml.depends>: ocaml, package(core, unix, threads), thread
<**/*.byte>: ocaml, byte, program, package(core, unix, threads), thread
<**/*.odoc>: ocaml, doc
<**/*.native>: ocaml, native, program, package(core, unix, threads), thread
<**/*.cma>: ocaml, byte, library, package(core, unix, threads), thread
<**/*.cmxa>: ocaml, native, library, package(core, unix, threads), thread
<**/*.cmo>: ocaml, byte
<**/*.cmi>: ocaml, byte, native
<**/*.cmx>: ocaml, native
